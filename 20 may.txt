use vitcampus;
CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) unique not null,
    FirstName varchar(255) NOT NULL,
    Age int
);
desc persons;

CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) unique not null,
    FirstName varchar(255) NOT NULL,
    Age int
);
desc persons;

insert into persons values (101,'Priyanshu','Kashyap',null);
select * from persons;

ALTER TABLE Persons
ADD UNIQUE (AGE);

ALTER TABLE Persons
DROP CONSTRAINT Age;


: CREATE TABLE Persons (
    ID int NOT NULL,
    LastName varchar(255) NOT NULL,
    FirstName varchar(255),
    Age int,
    CHECK (Age>=18)
);

ALTER TABLE Persons	
ADD CHECK (Age>=18);

select * from persons;

create table products(
pid int primary key,
pname varchar(100) not null,
prefno int not null unique
);

insert into products values (101,"Electronics", 19929);

select * from products;

alter table persons 
add primary key (id);
desc persons;

alter table persons 
drop primary key;


create table products(
pid int primary key,
pname varchar(100) not null,
prefNo int not null unique
);

desc products;

insert into products values (102,"furniture",19930);
select * from products;

ALTER TABLE Persons
ADD PRIMARY KEY (ID);

ALTER TABLE Persons
DROP PRIMARY KEY;
drop table products;
create table category(
cid int primary key,
cname varchar(100) not null
);

elect * from category;
drop table products;
CREATE TABLE products (
    pid int primary key,
    pname varchar(100) NOT NULL,
    cid int, 
    FOREIGN KEY (cid) REFERENCES category(cid)
);
desc products;

delete from category where cid=101;
update 


